<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match rewards>
    @type copy
    <store>
      @type file
      path /output/rewards
      compress gzip
      format json
    </store>
</match>

<match nginx>
    @type copy
    <store>
      @type file
      path /output/nginx
      compress gzip
      format json
    </store>
</match>