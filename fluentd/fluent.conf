<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match rewards>
    @type file
    path /output/rewards.log
</match>

<match nginx>
    @type file
    path /output/nginx.log
</match>