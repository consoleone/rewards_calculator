<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match rewards>
  @type elasticsearch
  host elasticsearch
  port 9300
  index_name rewards
  type_name logs
</match>

<match nginx>
  @type elasticsearch
  host elasticsearch
  port 9300
  index_name nginx
  type_name logs
</match>