<source>
  @type forward
  port 24224
  bind 0.0.0.0
</source>

<match rewards>
    @type file
    path /fluentd/log/rewards/
</match>

<match nginx>
    @type file
    path /fluentd/log/nginx/
</match>